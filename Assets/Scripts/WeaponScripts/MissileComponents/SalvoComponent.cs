using UnityEngine;

//unused as changes occur in stats and on weapon, not on missile itself.
public class SalvoComponent : BaseMissileComponent
{
    //salvo reduces size of missile (stats change)
    //however does not handle spawning other mini missiles here. that should be handled in the missile weapon.
}
